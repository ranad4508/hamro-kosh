org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Works around a Kotlin incremental-compiler crash
# ("this and base files have different roots") that happens on Windows
# when the project directory and the Gradle/pub caches live on different
# drive letters (e.g. project on D:, caches on C:). Safe to remove once
# this project and your Gradle/Kotlin caches are on the same drive.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
